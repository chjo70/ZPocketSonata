/*
 * Copyright (c) 2021 Wind River Systems, Inc.
 *
 * The right to copy, distribute, modify or otherwise make use
 * of this software may be licensed only pursuant to the terms
 * of an applicable Wind River license agreement.
 */

/*
 *  DO NOT MODIFY THIS FILE MANUALLY
 *
 *  This file is automatically generated from
 *  the method file (extended with *.m).
 *
 */

#include <vxWorks.h>
#include <hwif/vxBus.h>
#include <lstLib.h>
#include <hwif/vxbus/vxbLib.h>
#include <subsys/dma/vxbDmaLib.h>
#include <hwif/methods/vxbDmaMethod.h>

VXB_DEVMETHOD_DEF(vxbDmaChanAlloc, "VxBus DMA channel allocate method");

VXB_DMA_CHAN * VXB_DMA_CHAN_ALLOC (VXB_DEV_ID pDev)
    {
    vxbDmaChanAlloc_t * func = (vxbDmaChanAlloc_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaChanAlloc));

    if (func == NULL)
        return (VXB_DMA_CHAN *)VXB_NO_METHOD;

    return func (pDev);
    }


VXB_DEVMETHOD_DEF(vxbDmaChanAllocAdvance, "VxBus DMA channel allocate by advanced method");

VXB_DMA_CHAN * VXB_DMA_CHAN_ALLOC_ADVANCE (VXB_DEV_ID pDev, int chanNum, UINT32 uCap)
    {
    vxbDmaChanAllocAdvance_t * func = (vxbDmaChanAllocAdvance_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaChanAllocAdvance));

    if (func == NULL)
        return (VXB_DMA_CHAN *)NULL;

    return func (pDev, chanNum, uCap);
    }


VXB_DEVMETHOD_DEF(vxbDmaChanPreMemCpy, "VxBus prepare a memory copy DMA transfer method");

VXB_DMA_TX * VXB_DMA_CHAN_PRE_MEM_CPY (VXB_DEV_ID pDev, VXB_DMA_CHAN * pDmaChan, VIRT_ADDR src, VIRT_ADDR dest, size_t len, int flag)
    {
    vxbDmaChanPreMemCpy_t * func = (vxbDmaChanPreMemCpy_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaChanPreMemCpy));

    if (func == NULL)
        return (VXB_DMA_TX *)NULL;

    return func (pDev, pDmaChan, src, dest, len, flag);
    }


VXB_DEVMETHOD_DEF(vxbDmaChanPreScg, "VxBus prepare a scatter-gather DMA transfer method");

VXB_DMA_TX * VXB_DMA_CHAN_PRE_SCG (VXB_DEV_ID pDev, VXB_DMA_CHAN * pDmaChan, LIST * srcList, LIST * descList, size_t len, int flag)
    {
    vxbDmaChanPreScg_t * func = (vxbDmaChanPreScg_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaChanPreScg));

    if (func == NULL)
        return (VXB_DMA_TX *)NULL;

    return func (pDev, pDmaChan, srcList, descList, len, flag);
    }


VXB_DEVMETHOD_DEF(vxbDmaChanControl, "VxBus DMA channel control method");

STATUS VXB_DMA_CHAN_CONTROL (VXB_DEV_ID pDev, VXB_DMA_CHAN * pDmaChan, VXB_DMA_CTRL_CMD cmd, void * pArg)
    {
    vxbDmaChanControl_t * func = (vxbDmaChanControl_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaChanControl));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, pDmaChan, cmd, pArg);
    }


VXB_DEVMETHOD_DEF(vxbDmaChanFree, "VxBus DMA channel free method");

STATUS VXB_DMA_CHAN_FREE (VXB_DEV_ID pDev, VXB_DMA_CHAN * pDmaChan)
    {
    vxbDmaChanFree_t * func = (vxbDmaChanFree_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaChanFree));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, pDmaChan);
    }


