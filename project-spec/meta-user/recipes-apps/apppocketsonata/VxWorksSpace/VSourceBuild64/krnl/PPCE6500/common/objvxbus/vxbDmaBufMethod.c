/*
 * Copyright (c) 2021 Wind River Systems, Inc.
 *
 * The right to copy, distribute, modify or otherwise make use
 * of this software may be licensed only pursuant to the terms
 * of an applicable Wind River license agreement.
 */

/*
 *  DO NOT MODIFY THIS FILE MANUALLY
 *
 *  This file is automatically generated from
 *  the method file (extended with *.m).
 *
 */

#include <vxWorks.h>
#include <hwif/vxBus.h>
#include <hwif/util/vxbDmaBufLib.h>
#include <hwif/methods/vxbDmaBufMethod.h>

VXB_DEVMETHOD_DEF(vxbDmaBufTagGet, "VxBus DMA buffer tag create method");

VXB_DMA_TAG_ID VXB_DMA_BUF_TAG_GET (VXB_DEV_ID pDev, UINT32 regBaseIndex)
    {
    vxbDmaBufTagGet_t * func = (vxbDmaBufTagGet_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaBufTagGet));

    if (func == NULL)
        return (VXB_DMA_TAG_ID)NULL;

    return func (pDev, regBaseIndex);
    }


VXB_DEVMETHOD_DEF(vxbDmaBufMapCreate, "VxBus DMA buffer map create method");

VXB_DMA_MAP_ID VXB_DMA_BUF_MAP_CREATE (VXB_DEV_ID pDev, VXB_DEV_ID pCreator, VXB_DMA_TAG_ID dmaTagID, int flags, VXB_DMA_MAP_ID * ppMap)
    {
    vxbDmaBufMapCreate_t * func = (vxbDmaBufMapCreate_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaBufMapCreate));

    if (func == NULL)
        return (VXB_DMA_MAP_ID)NULL;

    return func (pDev, pCreator, dmaTagID, flags, ppMap);
    }


VXB_DEVMETHOD_DEF(vxbDmaBufMapDestroy, "VxBus DMA buffer map destroy method");

STATUS VXB_DMA_BUF_MAP_DESTROY (VXB_DEV_ID pDev, VXB_DMA_TAG_ID dmaTagID, VXB_DMA_MAP_ID map)
    {
    vxbDmaBufMapDestroy_t * func = (vxbDmaBufMapDestroy_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaBufMapDestroy));

    if (func == NULL)
        return (STATUS)VXB_NO_METHOD;

    return func (pDev, dmaTagID, map);
    }


VXB_DEVMETHOD_DEF(vxbDmaBufMemAlloc, "VxBus DMA buffer memory allocate method");

void * VXB_DMA_BUF_MEM_ALLOC (VXB_DEV_ID pDev, VXB_DMA_TAG_ID dmaTagID, void ** vaddr, int flags, VXB_DMA_MAP_ID map)
    {
    vxbDmaBufMemAlloc_t * func = (vxbDmaBufMemAlloc_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaBufMemAlloc));

    if (func == NULL)
        return (void *)NULL;

    return func (pDev, dmaTagID, vaddr, flags, map);
    }


VXB_DEVMETHOD_DEF(vxbDmaBufMemFree, "VxBus DMA buffer memory free method");

STATUS VXB_DMA_BUF_MEM_FREE (VXB_DEV_ID pDev, VXB_DMA_TAG_ID dmaTagID, void * vaddr, VXB_DMA_MAP_ID map)
    {
    vxbDmaBufMemFree_t * func = (vxbDmaBufMemFree_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(vxbDmaBufMemFree));

    if (func == NULL)
        return (STATUS)VXB_NO_METHOD;

    return func (pDev, dmaTagID, vaddr, map);
    }


