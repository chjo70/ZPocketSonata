/*
 * Copyright (c) 2021 Wind River Systems, Inc.
 *
 * The right to copy, distribute, modify or otherwise make use
 * of this software may be licensed only pursuant to the terms
 * of an applicable Wind River license agreement.
 */

/*
 *  DO NOT MODIFY THIS FILE MANUALLY
 *
 *  This file is automatically generated from
 *  the method file (extended with *.m).
 *
 */

#include <vxWorks.h>
#include <hwif/vxBus.h>
#include <hwif/methods/vxbMiiMethod.h>

VXB_DEVMETHOD_DEF(miiModeGet, "MII mode get");

STATUS MII_MODE_GET (VXB_DEV_ID pDev, UINT32 * mode, UINT32 * sts)
    {
    miiModeGet_t * func = (miiModeGet_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(miiModeGet));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, mode, sts);
    }


VXB_DEVMETHOD_DEF(miiModeSet, "MII mode set");

STATUS MII_MODE_SET (VXB_DEV_ID pDev, UINT32 mode)
    {
    miiModeSet_t * func = (miiModeSet_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(miiModeSet));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, mode);
    }


VXB_DEVMETHOD_DEF(miiMediaUpdate, "MII bus media update");

STATUS MII_MEDIA_UPDATE (VXB_DEV_ID pDev)
    {
    miiMediaUpdate_t * func = (miiMediaUpdate_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(miiMediaUpdate));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev);
    }


VXB_DEVMETHOD_DEF(miiMediaAttach, "MII bus new media attach");

STATUS MII_MEDIA_ATTACH (VXB_DEV_ID pDev, VXB_DEV_ID pPhy)
    {
    miiMediaAttach_t * func = (miiMediaAttach_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(miiMediaAttach));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, pPhy);
    }


VXB_DEVMETHOD_DEF(miiMediaDetach, "MII bus new media detach");

STATUS MII_MEDIA_DETACH (VXB_DEV_ID pDev, VXB_DEV_ID pPhy)
    {
    miiMediaDetach_t * func = (miiMediaDetach_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(miiMediaDetach));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, pPhy);
    }


VXB_DEVMETHOD_DEF(miiRead, "MII bus clause22 register read");

STATUS MII_READ (VXB_DEV_ID pDev, UINT8 phyAddr, UINT8 phyReg, UINT16 * regVal)
    {
    miiRead_t * func = (miiRead_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(miiRead));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, phyAddr, phyReg, regVal);
    }


VXB_DEVMETHOD_DEF(miiWrite, "MII bus clause22 register write");

STATUS MII_WRITE (VXB_DEV_ID pDev, UINT8 phyAddr, UINT8 phyReg, UINT16 regVal)
    {
    miiWrite_t * func = (miiWrite_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(miiWrite));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, phyAddr, phyReg, regVal);
    }


VXB_DEVMETHOD_DEF(mii45Read, "MII bus clause45 register read");

STATUS MII45_READ (VXB_DEV_ID pDev, UINT8 phyAddr, UINT8 devAddr, UINT16 phyReg, UINT16 * regVal)
    {
    mii45Read_t * func = (mii45Read_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(mii45Read));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, phyAddr, devAddr, phyReg, regVal);
    }


VXB_DEVMETHOD_DEF(mii45Write, "MII bus clause45 register write");

STATUS MII45_WRITE (VXB_DEV_ID pDev, UINT8 phyAddr, UINT8 devAddr, UINT16 phyReg, UINT16 regVal)
    {
    mii45Write_t * func = (mii45Write_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(mii45Write));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, phyAddr, devAddr, phyReg, regVal);
    }


VXB_DEVMETHOD_DEF(miiLpiModeGet, "phy power status read");

STATUS MII_LPI_MODE_GET (VXB_DEV_ID pDev, UINT16 * mode)
    {
    miiLpiModeGet_t * func = (miiLpiModeGet_t *) vxbDevMethodFind (pDev, VXB_DEVMETHOD_CALL(miiLpiModeGet));

    if (func == NULL)
        return (STATUS)ERROR;

    return func (pDev, mode);
    }


