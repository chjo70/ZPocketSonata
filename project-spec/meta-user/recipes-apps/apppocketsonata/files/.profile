alias l='ls -CF'
alias la='ls -A'
alias ll='ls -alF'
alias ls='ls --color=auto'

alias viq='vi /var/log/LOG/qDecoder.log'

alias cats='cat ~/sysconfig.ini'

alias h='history'

# mkdir /var/log/LOG

mkdir /var/rawdata
mount -t nfs 192.168.1.245:/d/rawdata /var/rawdata

# apppocketsonata
